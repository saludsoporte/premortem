<!DOCTYPE html>
<html>
  <head>
    <title>Sistema para Actas Premortem</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "loading", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg">
 <%= render "partials/menu" %>
    <nav class=" navbar navbar-expand-lg navbar-light degradado">
      <a class="navbar-brand btn btn-sm btn-outline-primary" style="font-size:15px" type="button" href="#" data-bs-toggle="offcanvas" data-bs-target="#offcanvasExample" aria-controls="offcanvasExample">
        <%# image_tag("OfiPartLogo.png", size: "35x35") %>
        Menu       
      </a>
      <%= image_tag("thumbnail_Logos-Sin_Límites_Mesa de trabajo 1.png",size:"250x50") %> &nbsp;   
      &nbsp; <div class="vl"></div>
    </nav>
    <div class="derecha " style="position:relative;top: -40px;"  >
      <div class="input-group">
        <%= link_to raw("#{image_tag("house-door-w.svg")} Inicio"),root_path,id: :inicio_btn,class: :"btn btn-sm btn-success" %>
        <%= link_to raw("#{image_tag("door-closed-fill.svg")} Cerrar Sesión"), destroy_user_session_path, data: { turbo_method: :delete},class: :"btn btn-sm btn-danger " %>
      </div>
    </div>
    <%= yield %>
  </body>
  <footer class="footer " >        
    <div class="position-absolute top-50 start-50 translate-middle">
      <% if @buzones.nil? == false %>
        <%= will_paginate(@buzones,:renderer => WillPaginate::ActionView::BootstrapLinkRenderer ) %>    
      <%  end %>
      <p style="font-size:13px;color:white">Servicios de Salud de San Luis Potosi</p>
    </div>
  </footer>
</html>
